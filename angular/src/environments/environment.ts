// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAyWysc8B6B0_yMd0OwkmRKOcqm4PBXsPA",
    authDomain: "automathon-bot.firebaseapp.com",
    databaseURL: "https://automathon-bot.firebaseio.com",
    projectId: "automathon-bot",
    storageBucket: "automathon-bot.appspot.com",
    messagingSenderId: "577912689316",
    appId: "1:577912689316:web:7cc042be3aef3b788f8c31"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
